# Local/private config goes in the include
[include]
    path = ~/.gitconfig.local

[core]
    editor = vim

[merge]
    tool = gvimdiff
    conflictstyle = diff3

# show diff in commit message
[commit]
    verbose = true
